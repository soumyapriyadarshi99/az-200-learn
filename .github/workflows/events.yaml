name: push-events
on: 
  push:
    branches:
      - main
      - dev
jobs:
  show-info:
    runs-on: ubuntu-latest
    steps: 
      - name: info
        run: |
          echo "This is a test run"
  build-info:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: |
          echo "Building the project..."